"""Local web UI package for Blackjack Coach Pro Demo (v2.0.0).

Contains the optional Streamlit Web Coach UI (``web/streamlit_app.py``). The UI
is a thin presentation layer over :mod:`app.web_adapter`; the engine, CLI, and
recommendations are unchanged. Streamlit is an optional dependency - install it
with ``pip install -e ".[web]"``.
"""
